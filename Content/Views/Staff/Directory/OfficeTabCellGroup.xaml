<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wwrc_maui.Content.Views.Staff.Directory.OfficeTabCellGroup">

    <VerticalStackLayout HorizontalOptions="Fill" Spacing="0">
        <HorizontalStackLayout HorizontalOptions="Start" Spacing="10" Padding="0,8">
            <Image Source="{AppThemeBinding Light=ic_more, Dark=ic_more_light}" HeightRequest="20"
                   Aspect="AspectFit" HorizontalOptions="Start" VerticalOptions="Center"/>
            <Label Text="{Binding Key}" HorizontalOptions="Start" HorizontalTextAlignment="Start"
                   VerticalOptions="Center" VerticalTextAlignment="Center"/>
        </HorizontalStackLayout>
        <BoxView HeightRequest="1" HorizontalOptions="Fill"/>
    </VerticalStackLayout>
    
</StackLayout>
