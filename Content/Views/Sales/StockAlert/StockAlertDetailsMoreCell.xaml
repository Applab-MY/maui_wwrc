<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="wwrc_maui.Content.Views.Sales.StockAlert.StockAlertDetailsMoreCell">

    <Grid HorizontalOptions="Fill" ColumnDefinitions="0.6*,0.4*" RowDefinitions="Auto"
          ColumnSpacing="10" Padding="15,10">
        <VerticalStackLayout HorizontalOptions="Fill" Spacing="0">
            <Label Text="{Binding CardCode}" FontAttributes="Bold" HorizontalOptions="Start" 
                   HorizontalTextAlignment="Start" VerticalOptions="Center"
                   VerticalTextAlignment="Center"/>
            <Label Text="{Binding CardName}" HorizontalOptions="Start"
                   HorizontalTextAlignment="Start" VerticalOptions="Center"
                   VerticalTextAlignment="Center"/>
        </VerticalStackLayout>
        <Label Text="{Binding Commited}" FontAttributes="Bold" HorizontalOptions="End" 
               HorizontalTextAlignment="End" VerticalOptions="Center" Grid.Column="1"
               VerticalTextAlignment="Center"/>
    </Grid>

</ViewCell>
